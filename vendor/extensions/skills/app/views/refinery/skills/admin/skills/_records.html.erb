<%= render 'refinery/admin/search_header', :url => refinery.skills_admin_skills_path %>
<div class='pagination_container'>
  <% if @skills.any? %>
    <%= render 'skills' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
