<%= render 'refinery/admin/search_header', :url => refinery.employees_admin_departments_path %>
<div class='pagination_container'>
  <% if @departments.any? %>
    <%= render 'departments' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
