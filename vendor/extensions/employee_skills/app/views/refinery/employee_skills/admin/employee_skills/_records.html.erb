<%= render 'refinery/admin/search_header', :url => refinery.employee_skills_admin_employee_skills_path %>
<div class='pagination_container'>
  <% if @employee_skills.any? %>
    <%= render 'employee_skills' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
